package com.crackedgames.craftics.api;

/**
 * Factory methods for composing common {@link EventHandler} patterns.
 *
 * <p>Planned templates (gamble, giveReward, ambush, spawnTrader) will wrap the most
 * frequent between-level event shapes so addon authors do not have to reimplement them.
 * This class is a placeholder — all factories are pending implementation.
 *
 * @since 0.2.0
 */
public final class EventTemplates {
    private EventTemplates() {}

    // TODO: gamble, giveReward, ambush, spawnTrader templates
    // These will wrap common event patterns for addon convenience
}
